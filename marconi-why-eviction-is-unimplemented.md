# 为什么 Marconi 的"驱逐"部分全社区都没人实现

> 记录日期：2026-09-20
> 结论先行：**"存"是救火，所以一堆人顺手做了；"驱逐"是精装修，而且方案要求先拆承重墙、还要接受"短请求变慢"，所以谁都不愿开工。**

---

## 0. 背景：Marconi 的两半

Marconi 这篇论文（MLSys 2025，[arXiv:2411.19379](https://arxiv.org/abs/2411.19379)）解决的是"混合模型（Attention + SSM）上没法做前缀缓存"的问题，它的贡献可以粗暴地分成两半：

| | 存（在哪存） | 驱逐（扔谁） |
|---|---|---|
| 论文主张 | 只缓存两处：大家共用的开头（共享前缀）、对话的结尾（last decoded state） | 内存满时，按"能省多少计算 ÷ 占多少内存"排序淘汰（FLOP 效率），而不是纯 LRU |
| 有没有官方实现 | ❌ 没有。是一堆各自独立的 PR 拼出来的 | ❌ 完全没有 |
| 方法是否等于论文 | ❌ 不是。vLLM 用"事后观察"（复印件命中了但笔记没跟上 → 补一份），论文用"提前猜"（拿新请求去历史树上试插，预测分叉点） | — |
| 完成度 | 约 70~80%，还剩几个正确性尾巴 | 0%，只有两个卡住的尝试 |
| 为什么是这个状态 | 不做就没法用，改动小、风险低 | 不做也能用；动地基；必然牺牲一部分请求；无人有动机；连度量都没有 |

**关于"没有官方实现"**：没人立项说"我们来复现 Marconi"。实际是工程师 A 要支持 Qwen3.5、发现 Mamba 模型根本没法前缀缓存 → 做了"块对齐存储"（[#30877](https://github.com/vllm-project/vllm/pull/30877)）；工程师 B 发现生产里 system prompt 命中率 0 → 做了"共享前缀检测"（[#37898](https://github.com/vllm-project/vllm/pull/37898)），顺手在标题里写了 "Marconi-style"；工程师 C 内存不够 → 做了"稀疏保留"，顺带把 B 的位置一起保住（[#47782](https://github.com/vllm-project/vllm/pull/47782)）。所以更准确的说法是：**vLLM 独立收敛到了和论文类似的效果，然后借用了论文的名字**，而不是移植了论文。

---

## 1. 七个原因，从最实在到最微妙

### 1. "存"是不做就没法用，"驱逐"是不做也能用

不做"存"，混合模型的前缀缓存命中率就是 0%，这是**功能性缺陷**，必须修。
不做"驱逐"，系统照样跑，只是命中率低一点——这是**优化**。

工程资源永远先修功能，后做优化。

### 2. "存"的改动能关在一个小盒子里，"驱逐"改的是全局策略

- "存"改什么？调度器切一下本次读题的长度（#37898 里 +21 行）、coordinator 多返回一个数字（+6 行）。影响面小、好测、好回滚。
- "驱逐"改什么？**内存满时决定扔谁**。这是所有模型、所有请求都会走到的公共路径。改错一次，全体用户性能倒退。

maintainer 看到"改全局驱逐策略"的 PR，天然抗拒。

### 3. "驱逐"需要一份现在系统里没有的账本

论文要算"这份缓存省了多少计算 ÷ 占了多少内存"。但 vLLM 的块池里，块就是一个个固定大小的格子、按哈希索引，**它并不知道"这个格子代表多长的序列、省了多少计算"**。

要算这个分数，就得在最核心的缓存代码里给每个块加记账，还要处理这些麻烦事：

- 一个块可能被多个请求共用（前缀共享），怎么分摊收益；
- attention 块和 mamba 块语义不同（前者大小随长度变，后者大小固定）；
- 外部存储（PD 分离 / AscendStore / Mooncake）上的块怎么参与计算。

**这是动地基，不是贴补丁。**

### 4. 论文的策略天生是"牺牲一部分人"，而开源引擎最怕这个

Marconi 的核心取舍是：**放弃短请求的命中率，保住长请求**。论文测出来短请求只亏 2.1ms，长请求赚几百毫秒，在企业自己的推理服务里这个取舍可以做。

但在 vLLM 这种通用引擎里，这会被定义成 **regression**——"你凭什么让我的短请求变慢？"。

maintainer 只会收"绝对不退化"的改动，而这条路做不到绝对不退化。

### 5. 收益已经被"存"和"稀疏保留"吃掉大半了

论文里"驱逐比 LRU 好 19%~219%"这个数，是在**细粒度存档 + 缓存很紧张**的前提下测出来的。

上游后来选了另一条路：**稀疏保留**（干脆少存点、只留关键位置），缓存竞争一下子没那么激烈了，**驱逐能榨出来的额外收益就变小了**。

（这一条是逻辑推断，论文没有直接测"稀疏保留之上再做驱逐"。）

### 6. 没人有动机

- **学术上**：这套已经在 MLSys 2025 发过论文了，再实现一遍不算新工作；
- **工业上**：修 bug、支持新模型、优化已有的稀疏保留，KPI 都比"实现论文的驱逐"高；
- **而且连度量都没有**：昇腾那边 [#16639](https://github.com/vllm-project/vllm-ascend/issues/16639) 显示命中计数恒为 0，上游 [#52527](https://github.com/vllm-project/vllm/pull/52527) 还在补"怎么统计丢掉的复用"。

**看不见收益的东西，没人敢投入。**

### 7. 有一点点尝试，但都卡住了

这恰好是最直接的证据：

| 尝试 | 内容 | 状态 |
|---|---|---|
| [#55403](https://github.com/vllm-project/vllm/pull/55403) | 给块标优先级，让被屏蔽的缓存还能"碰巧命中" | open，标着 **needs-rebase** |
| [#37003](https://github.com/vllm-project/vllm/issues/37003) | RFC：带优先级的驱逐 API | open，没人推进 |
| [tracking issue #26201](https://github.com/vllm-project/vllm/issues/26201) | 混合模型前缀缓存的待办清单 | 列了 5 个"存"的待办，**一条驱逐的都没有** |

连"最边缘的驱逐尝试"都缺人 review，说明它确实不在社区优先级里。

---

## 2. 一句话总结

> **存的那半是"救火"，所以一堆人顺手做了；驱逐那半是"精装修"，而且装修方案要求先拆掉一堵承重墙（块池的记账），还要接受"短请求变慢"，所以谁都不愿意开工。**

---

## 3. 如果想做这块，两个前提

1. **先能看见**：把命中率 / 丢掉的复用量指标做出来（上游 #52527、昇腾 #16639）。没有度量，做完也证明不了有效。
2. **别一上来就动主路径**：先用论文 artifact 里的真实 trace 做**离线仿真**（自带策略仿真脚本，纯 CPU 可跑），把"在我们关心的 workload 上到底能提升多少"跑出来。有数据之后，再作为**默认关闭的开关**提 PR——这样才有可能被接受。
